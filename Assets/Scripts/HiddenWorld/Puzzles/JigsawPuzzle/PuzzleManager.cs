using System.Collections;
using System.Collections.Generic;
using System.Diagnostics.CodeAnalysis;
using UnityEngine;
using HiddenWorld.Helpers;

namespace HiddenWorld.Puzzle
{
    public class PuzzleManager : MonoBehaviour
    {
        // Need private list of puzzles.  new puzzles must be added to this list during dev
        // Puzzle list holds puzzle solve state: solved/not solved
        // Need setter to set puzzle solved state
        // Need cumulative puzzle solve progress

        // May require a UI interface method for things like puzzle: progress, timer, orientation, etc.



        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}
