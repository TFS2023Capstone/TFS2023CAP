 //game tags, Material Overlay
    public Material colourOverlay1;
    private Material[] noOverlay1;
    public Material colourOverlay2;
    private Material[] noOverlay2;
    public Material colourOverlay3;
    private Material[] noOverlay3;
    private bool isOverlayed = false;
    private GameObject EnemyTag;
    private GameObject PickupTag;
    private GameObject CheckpointTag;




        //Trying to create the same function as with the environment here

        //noOverlay1 = GameObject.FindGameObjectWithTag("Enemy");
        //Renderer[] renderers = noOverlay1<Renderer>();
        //originalMaterials = new Material[renderers.Length];
        //for (int i = 0; i < renderers.Length; i++)
        //{
        //    originalMaterials[i] = renderers[i].material;
        //}




        if (isToggled = true && Input.GetKeyUp(KeyCode.Alpha1))
        {
            isOverlayed = !isOverlayed;
            EnemyTag = GameObject.FindGameObjectWithTag("Enemy");
            Renderer renderers = EnemyTag.GetComponent<Renderer>();
            //for (int i = 0; i < renderers.Length; i++)
            //{
            //    renderers[i].material = isOverlayed ? colourOverlay1 : noOverlay1[i];
            //}
            renderers.material = colourOverlay1;
        }


